<div class="container">
  <div class= "row">
    <div class="col-sm-8 px-sm-0 offset-sm-1">
      <h4>注文履歴詳細</h4>
    </div>
  </div>
</div>

<tr>
  <p>購入者<%= @order.shipping_name %></p>
  <p>注文日<%= @order.created_at.strftime('%Y/%m/%d')%></p>
  <p>配送先  〒<%= @order.shipping_postal_code + @order.shipping_address%><br>
     <%= @order.shipping_name%>
  </p>
  
  <p>支払方法<%= @order.payment_method_i18n %></p>
</tr>